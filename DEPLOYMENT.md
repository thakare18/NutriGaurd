# NutriGuard Deployment

Primary deployment guide has been moved to:

- `config/docs/DEPLOYMENT.md`

This root file is kept for compatibility and quick discovery.

## Quick Start

```bash
pip install -r requirements.txt
python run.py
```

For platform deployment details, use `config/docs/DEPLOYMENT.md`.
